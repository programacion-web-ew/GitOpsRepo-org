apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend-deployment
  namespace: biblioteca
spec:
  replicas: 1
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
    spec:
      containers:
<<<<<<< HEAD
        - name: backend-app # Nombre del contenedor
          image: ewdeve/pgbackend:2ac8f7e
=======
        - name: backend-app
          image: ewdeve/pgbackend:latest
>>>>>>> 124e297 (change endpoint bd)
          ports:
            - containerPort: 8000
          env:
            - name: DB_URL
              value: "jdbc:mysql://mysql-svc:3306/biblioteca1?useSSL=false&serverTimezone=UTC"
            - name: DB_USERNAME
              valueFrom:
                secretKeyRef:
                  name: mysql-credentials
                  key: mysql-user
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: mysql-credentials
                  key: mysql-password